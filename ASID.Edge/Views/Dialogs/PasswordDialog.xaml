<Window x:Class="ASID.Edge.Views.Dialogs.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Required"
        Width="380"
        Height="250"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="🔒 Password Required"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#0B3A66"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="Enter your password to flag Non-Conformance:"
                   FontSize="13"
                   TextAlignment="Center"
                   Margin="0,0,0,10"/>

        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="2"
                     Height="38"
                     FontSize="16"
                     Margin="0,0,0,6"
                     KeyDown="PasswordInput_KeyDown"/>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Foreground="#D32F2F"
                   FontSize="12"
                   TextAlignment="Center"
                   Margin="0,0,0,5"/>

        <DockPanel Grid.Row="4" VerticalAlignment="Bottom">
            <Button Content="Cancel"
                    Width="80"
                    Height="32"
                    DockPanel.Dock="Left"
                    Click="Cancel_Click"/>

            <Button Content="Confirm"
                    Width="90"
                    Height="32"
                    Background="#0B3A66"
                    Foreground="White"
                    FontWeight="SemiBold"
                    DockPanel.Dock="Right"
                    HorizontalAlignment="Right"
                    Click="Confirm_Click"/>
        </DockPanel>
    </Grid>
</Window>
