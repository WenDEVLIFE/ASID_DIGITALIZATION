<UserControl x:Class="ASID.Edge.Views.Controls.LaneSequenceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="420">
    <Border BorderBrush="#333" BorderThickness="1.5" Background="White" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#0B3A66" Padding="8,6" HorizontalAlignment="Stretch">
                <TextBlock Text="LANE SEQUENCE" Foreground="White" FontWeight="Bold" FontSize="15"
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Lane Status Legend -->
            <Grid Grid.Row="1" Margin="8,8,8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#2ECC71" Width="20" Height="16" CornerRadius="2"
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Vacant" FontWeight="SemiBold" FontSize="11"
                           VerticalAlignment="Center"/>

                <Border Grid.Column="2" Background="#E74C3C" Width="20" Height="16" CornerRadius="2"
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="Full" FontWeight="SemiBold" FontSize="11"
                           VerticalAlignment="Center"/>

                <Border Grid.Column="4" Background="#BDC3C7" Width="20" Height="16" CornerRadius="2"
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Column="5" Text="Not Assigned" FontWeight="SemiBold" FontSize="11"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Lane Grid (scrollable) -->
            <Border Grid.Row="2" BorderBrush="#CCC" BorderThickness="1" Margin="0,4,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid x:Name="LaneGrid" Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel x:Name="ColumnA" Grid.Column="0"/>
                        <StackPanel x:Name="ColumnB" Grid.Column="2"/>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
