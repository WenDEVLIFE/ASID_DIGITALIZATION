<UserControl x:Class="ASID.Edge.Views.Controls.TransactionHistoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900"
             d:Background="LightGray">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <Border Grid.Row="0"
                Background="#0B3A66"
                Padding="8">
            <TextBlock Text="PU SUPERMARKET TRANSACTION HISTORY"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="15"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- ================= TOOLBAR ================= -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT : Sort Buttons -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Click="OnSortByModelClick"
                        Margin="5" Padding="10,5" Background="#E0E0E0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Model"/>
                        <TextBlock Text=" &#x25B2;&#x25BC;"/>
                    </StackPanel>
                </Button>

                <Button Click="OnSortByDateClick"
                        Margin="5" Padding="10,5" Background="#E0E0E0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Date"/>
                        <TextBlock Text=" &#x25B2;&#x25BC;"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- CENTER : Search Bar -->
            <Grid Grid.Column="1" Margin="10,0">
                <Border Background="White"
                        BorderBrush="#CCCCCC"
                        BorderThickness="1"
                        CornerRadius="3"
                        Padding="2">
                    <DockPanel>
                        <TextBlock Text="&#x1F50D;"
                                   DockPanel.Dock="Left"
                                   VerticalAlignment="Center"
                                   Margin="6,0,4,0"
                                   FontSize="13"
                                   Foreground="#888"/>
                        <TextBox x:Name="TxtSearch"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 Padding="2"
                                 FontSize="13"
                                 TextChanged="TxtSearch_TextChanged"/>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- RIGHT : Action Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="NonConformance"
                        Click="NonConformance_Click"
                        Margin="5" Padding="12,5"
                        Background="#D32F2F" Foreground="White"
                        FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x26A0; " FontSize="14"/>
                        <TextBlock Text="Suspected NC"/>
                    </StackPanel>
                </Button>

                <Button x:Name="QAReview"
                        Click="QAReview_Click"
                        Margin="5" Padding="12,5"
                        Background="#0B3A66" Foreground="White"
                        FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x1F50D; " FontSize="14"/>
                        <TextBlock Text="QA Review NC"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnOverride"
                        Click="Override_Click"
                        Margin="5" Padding="12,5"
                        Background="#6A1B9A" Foreground="White"
                        FontWeight="SemiBold" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x1F5D1; " FontSize="14"/>
                        <TextBlock Text="Override"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- ================= STATUS BAR ================= -->
        <Border Grid.Row="2" Margin="5" Padding="6"
                BorderBrush="#CFCFCF" BorderThickness="1"
                Background="#F5F5F5">
            <DockPanel>
                <TextBlock Text="Records : " FontWeight="Bold"/>
                <TextBlock x:Name="TxtRecordCount" Text="0"/>
                <TextBlock Margin="25,0,0,0" Text="Last Refresh : " FontWeight="Bold"/>
                <TextBlock x:Name="TxtLastRefresh" Text="--"/>
            </DockPanel>
        </Border>

        <!-- ================= TRANSACTION GRID ================= -->
        <DataGrid x:Name="TransactionGrid"
                  Grid.Row="3"
                  Margin="5"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  RowBackground="White"
                  AlternatingRowBackground="#EAEAEA"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsNCConfirmed}" Value="True">
                            <Setter Property="Background" Value="#FFCDD2"/>
                            <Setter Property="Foreground" Value="#B71C1C"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSuspectedNC}" Value="True">
                            <Setter Property="Background" Value="#FFF9C4"/>
                            <Setter Property="Foreground" Value="#E65100"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                <DataGridTextColumn Header="Part No" Binding="{Binding PartNo}" Width="120"/>
                <DataGridTextColumn Header="Serial No" Binding="{Binding SerialNo}" Width="130"/>
                <DataGridTextColumn Header="SNP" Binding="{Binding SNP}" Width="50"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding OperatorId}" Width="100"/>
                <DataGridTextColumn Header="Line No" Binding="{Binding LineNo}" Width="70"/>
                <DataGridTextColumn Header="Trolley No" Binding="{Binding TrolleyNo}" Width="80"/>
                <DataGridTextColumn Header="Lane No" Binding="{Binding LaneNo}" Width="70"/>
                <DataGridTextColumn Header="Remarks / NC" Binding="{Binding NCRemarks}" Width="120"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="90"/>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
